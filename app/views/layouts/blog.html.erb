<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= @page_title %> </title>
    <%= csrf_meta_tags %>
    <meta name = "keywords" content "<%= @seo_keywords %>" />
    <%= csp_meta_tag %>
    <!--  stylesheet_link_tag  app/assets/stylesheets/ dizinin içindeki css dosyalarına erişir-->
    <%= stylesheet_link_tag    'blogs', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    

   <!-- container  -->
    <main role="main" class="container"> 
    
     <%= render 'shared/blog_masthead' %>
    <!-- navigaston başlama  -->
      <%= render 'shared/blog_nav' %>
    <!-- navigaston bitiş  -->
    <div class="jumbotron p-3 p-md-5 text-white rounded bg-dark">
        <div class="col-md-6 px-0">
          <h1 class="display-4 font-italic">Today's Blogs</h1>
          <p class="lead my-3">Kekolar mibunlara çakacak ke zıbbini al ke zıbbini al</p>
          <p class="lead mb-0"><a href="#" class="text-white font-weight-bold">Continue reading...</a></p>
        </div>
      </div>
     


    
      <div class="row">
        <header class="blog-header py-3">
        <div class="row flex-nowrap justify-content-between align-items-center">
          <div class="col-4 pt-1">
            <!--a class="text-muted" href="#">Subscribe</a>
          </div>
          <div class="col-4 text-center">
            <a class="blog-header-logo text-dark" href="#">Large</a>
          </div>
          <div class="col-4 d-flex justify-content-end align-items-center">
            <a class="text-muted" href="#">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="mx-3"><circle cx="10.5" cy="10.5" r="7.5"></circle><line x1="21" y1="21" x2="15.8" y2="15.8"></line></svg>
            </a>
            <a class="btn btn-sm btn-outline-secondary" href="#">Sign up</a-->
          </div>
        </div>
      </header>
      
   
      <div class="row mb-2">
        <div class="col-md-6">
          <div class="card flex-md-row mb-4 shadow-sm h-md-250">
            <div class="card-body d-flex flex-column align-items-start">
              <strong class="d-inline-block mb-2 text-primary"><%=Blog.first.topic.title%> </strong>
              <h3 class="mb-0">
                <a class="text-dark" href="#"><%=Blog.first.title %></a>
              </h3>
              <div class="mb-1 text-muted"><%=Blog.first.created_at.strftime("%d %B")%></div>
              <p class="card-text mb-auto"><%= truncate Blog.first.body.squish, length: 125 %></p>
              <a href="#"><%= link_to 'Continue reading', Blog.first %></a>
            </div>
            <!--img class="card-img-right flex-auto d-none d-lg-block" data-src="holder.js/200x250?theme=thumb" alt="Card image cap"-->
          </div>
        </div>
        <div class="col-md-6">
          <div class="card flex-md-row mb-4 shadow-sm h-md-250">
            <div class="card-body d-flex flex-column align-items-start">
              <strong class="d-inline-block mb-2 text-success"> <%=Blog.last.topic.title%></strong>
              <h3 class="mb-0">
                <a class="text-dark" href="#"><%=Blog.last.title %></a>
              </h3>
              <div class="mb-1 text-muted"><%=Blog.last.created_at.strftime("%d %B")%></div>
              <p class="card-text mb-auto"><%= truncate Blog.last.body.squish, length: 125 %></p>
              <a href="#"><%= link_to 'Continue reading', Blog.last %></a>
            </div>
            <!--img class="card-img-right flex-auto d-none d-lg-block" data-src="holder.js/200x250?theme=thumb" alt="Card image cap"-->
          </div>
        </div>
      </div>
    </div>
  </main>


    <!-- containers -->






     
    <!-- current_user.is_a?(User) application_controller dosyasında override ettğimiz current_user metodu devise classına ait user mı yoksa oluşturduğumuz OpenStruct classına ait guest_user mı onu belirlemek için kullanıyoruz current_user.name komutuyla çalışan metodun hangi sınıfa ait olduğunu bulabiliriz -->
    <!--
    ******burdaki if döngüsü kaldırıldı çünkü view dosyalarının içinde saf ruby kodları yazmak iyi bir çalışma biçimi değildir
    onun yerine application_helper dosyasının içinde oluşturulan login_helper metodu kullanıldı


    <%#if current_user.is_a?(User) %>
      <%#= link_to "Logout", destroy_user_session_path, method: :delete %>
    <%# else %>
      <%#= link_to "Register", new_user_registration_path%>
      <%#= link_to "Login",  new_user_session_path%>
     
    <%#end %>  -->
    <%= yield %>
    <div class="container">
      <!--div class="row">
        
        <#%= render 'shared/blog_sidebar' %>
         
      </div-->
      <%= render 'shared/blog_footer' %>
    </div>
     
        
        
        
      
  </body>
</html>
