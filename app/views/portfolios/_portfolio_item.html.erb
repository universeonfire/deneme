<h2><%= link_to portfolio_item.title ,portfolio_show_path(portfolio_item) %></h2>
<p><%= portfolio_item.subtitle %></p>
<p><%= portfolio_item.body  %></p>
<%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %>
<p><%= link_to 'Sil', portfolio_path(portfolio_item), method: :delete, data: { confirm: 'Kayıt Silinecektir Emin Misiniz?' }  if logged_in?(:site_admin)  %></p>
<%= link_to "Düzenle",edit_portfolio_path(portfolio_item)  if logged_in?(:site_admin)  %>
<br>